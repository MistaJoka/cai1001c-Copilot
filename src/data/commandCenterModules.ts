/**
 * @deprecated Import {@link COMMAND_CENTER_LAYOUT_MODULES} from `@/lib/layout-demo-data` instead.
 * Thin adapter preserved so legacy `@/data/commandCenterModules` paths keep working.
 */

export type { CommandCenterModuleDef } from "@/lib/layout-demo-data";
export { COMMAND_CENTER_LAYOUT_MODULES as COMMAND_CENTER_MODULES } from "@/lib/layout-demo-data";
